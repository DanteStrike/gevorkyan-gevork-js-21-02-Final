enum ErrorMessages {
  PATH_NOT_FOUND = `Path not found`,
  INTERNAL_SERVER_ERROR = `Internal server error`,
  THIRD_PARTY_TIMEOUT_ERROR = `Third party timeout error`,
}

export default ErrorMessages;
