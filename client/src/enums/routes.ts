const enum RoutePath {
  BASE = `/`,
  REGISTRATION = `/registration`,
  LOGIN = `/login`,
  PROFILE = `/profile`,
  USERS = `/users`,
  POSTS = `/posts`,
  DENIED = `/denied`,
}

export default RoutePath;
